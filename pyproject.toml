[project]
name = "data-quality-observability"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2",
    "duckdb>=1.0",
    "pandera>=0.20",
    "streamlit>=1.38",
    "plotly>=5.24",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
